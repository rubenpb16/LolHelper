# Incluye todas las dependencias de producción
-r requirements.txt

# ── Tests ─────────────────────────────────────────────────────
pytest>=8.0
httpx>=0.27          # cliente HTTP para TestClient de FastAPI
pytest-env>=1.1      # permite setear env vars en pytest.ini
